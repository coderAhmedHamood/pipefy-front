# نظام التقارير الجديد - صفحة التقارير بثلاث تبويبات

## 📋 نظرة عامة

تم إعادة تصميم صفحة التقارير بالكامل لتصبح أكثر احترافية وسهولة في الاستخدام. النظام الجديد يحتوي على **ثلاث تبويبات** مع تركيز على تبويبة العمليات التي تعرض تقارير شاملة ومفصلة.

## ✨ الميزات الرئيسية

### 1. **ثلاث تبويبات منظمة**
- ✅ **تقارير العمليات**: تقارير شاملة لكل عملية
- ✅ **تقارير المستخدمين**: (قيد التطوير)
- ✅ **قيد التطوير**: للميزات المستقبلية

### 2. **تبويبة العمليات**

#### أ. **قائمة العمليات**
- عرض جميع العمليات في شكل بطاقات جميلة
- جلب البيانات من `GET /api/processes`
- معلومات كل عملية:
  - الاسم والوصف
  - اللون والأيقونة
  - الحالة (نشط/غير نشط)
- تصميم تفاعلي مع hover effects

#### ب. **تقرير العملية التفصيلي**
عند الضغط على عملية، يتم:
1. استدعاء `GET /api/reports/process/{process_id}`
2. عرض تقرير شامل يحتوي على:

**الإحصائيات الأساسية:**
- إجمالي التذاكر
- التذاكر المكتملة مع معدل الإنجاز
- التذاكر المتأخرة مع النسبة المئوية
- متوسط وقت الإنجاز بالساعات

**توزيع التذاكر حسب المرحلة:**
- عرض ديناميكي لجميع المراحل
- عدد التذاكر في كل مرحلة
- النسبة المئوية
- شريط تقدم مرئي
- ألوان مميزة لكل مرحلة

**توزيع حسب الأولوية:**
- عاجل جداً (أحمر)
- عاجل (برتقالي)
- متوسط (أصفر)
- منخفض (أخضر)
- عدد ونسبة كل أولوية

**أفضل الموظفين أداءً:**
- ترتيب الموظفين حسب عدد التذاكر المكتملة
- عرض جميل مع أرقام وأيقونات
- تصميم متدرج من الذهبي إلى البرتقالي

## 🎨 التصميم

### الألوان والأيقونات
```tsx
// الإحصائيات
- إجمالي التذاكر: أزرق (BarChart3)
- مكتملة: أخضر (CheckCircle)
- متأخرة: أحمر (AlertTriangle)
- متوسط الإنجاز: بنفسجي (Clock)

// الأقسام
- توزيع المراحل: أزرق (Target)
- توزيع الأولوية: برتقالي (AlertTriangle)
- أفضل الموظفين: ذهبي (Award)
```

### التخطيط
```
┌─────────────────────────────────────────────────┐
│ Header: العنوان + زر التحديث                    │
├─────────────────────────────────────────────────┤
│ Tabs: العمليات | المستخدمين | قيد التطوير       │
├─────────────────────────────────────────────────┤
│                                                 │
│  [قائمة العمليات]  أو  [تقرير العملية]        │
│                                                 │
│  - بطاقات العمليات                             │
│  - أو تقرير مفصل مع إحصائيات                   │
│                                                 │
└─────────────────────────────────────────────────┘
```

## 🔌 API Endpoints المستخدمة

### 1. جلب جميع العمليات
```http
GET /api/processes
Authorization: Bearer {token}

Response:
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "name": "اسم العملية",
      "description": "وصف العملية",
      "color": "bg-blue-500",
      "icon": "FolderOpen",
      "is_active": true,
      "created_at": "2025-01-01T00:00:00.000Z",
      "stages": [...]
    }
  ]
}
```

### 2. جلب تقرير عملية معينة
```http
GET /api/reports/process/{process_id}
Authorization: Bearer {token}

Query Parameters (اختياري):
- date_from: تاريخ البداية (افتراضياً آخر 30 يوم)
- date_to: تاريخ النهاية (افتراضياً الآن)

Response:
{
  "success": true,
  "data": {
    "process": {
      "id": "uuid",
      "name": "اسم العملية",
      "description": "وصف",
      "color": "bg-blue-500"
    },
    "period": {
      "from": "2025-01-01",
      "to": "2025-01-31"
    },
    "basic_stats": {
      "total_tickets": 150,
      "open_tickets": 80,
      "completed_tickets": 70,
      "overdue_tickets": 15,
      "avg_completion_hours": 48.5
    },
    "stage_distribution": [
      {
        "stage_name": "مرحلة جديدة",
        "ticket_count": 50,
        "percentage": 33.33
      }
    ],
    "priority_distribution": [
      {
        "priority": "urgent",
        "count": 20,
        "percentage": 13.33
      }
    ],
    "completion_rate": {
      "total": 150,
      "completed": 70,
      "rate": 46.67
    },
    "top_performers": [
      {
        "user_name": "أحمد محمد",
        "completed_tickets": 25
      }
    ]
  }
}
```

## 📁 الملفات المعدلة

### 1. **src/components/reports/ReportsManager.tsx**
```tsx
// المكون الرئيسي للتقارير
export const ReportsManager: React.FC = () => {
  const [activeTab, setActiveTab] = useState<TabType>('processes');
  const [processes, setProcesses] = useState<Process[]>([]);
  const [selectedProcess, setSelectedProcess] = useState<Process | null>(null);
  const [processReport, setProcessReport] = useState<ProcessReport | null>(null);
  
  // جلب العمليات
  const fetchProcesses = async () => { ... }
  
  // جلب تقرير عملية
  const fetchProcessReport = async (processId: string) => { ... }
  
  // عرض التبويبات والمحتوى
  return ( ... )
}
```

## 🎯 سير العمل (Workflow)

### 1. **عند فتح صفحة التقارير**
```
1. تحميل الصفحة
   ↓
2. استدعاء fetchProcesses()
   ↓
3. عرض قائمة العمليات في بطاقات
```

### 2. **عند اختيار عملية**
```
1. المستخدم يضغط على بطاقة عملية
   ↓
2. استدعاء handleProcessClick(process)
   ↓
3. تعيين selectedProcess
   ↓
4. استدعاء fetchProcessReport(process.id)
   ↓
5. عرض التقرير المفصل
```

### 3. **العودة إلى القائمة**
```
1. المستخدم يضغط "العودة إلى القائمة"
   ↓
2. استدعاء handleBackToList()
   ↓
3. إعادة تعيين selectedProcess و processReport
   ↓
4. عرض قائمة العمليات
```

## 💡 مثال على الاستخدام

### في الكود
```tsx
// جلب العمليات
const fetchProcesses = async () => {
  const token = localStorage.getItem('auth_token');
  const response = await fetch('http://localhost:3000/api/processes', {
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    }
  });
  
  if (response.ok) {
    const result = await response.json();
    setProcesses(result.data);
  }
};

// جلب تقرير عملية
const fetchProcessReport = async (processId: string) => {
  const token = localStorage.getItem('auth_token');
  const response = await fetch(
    `http://localhost:3000/api/reports/process/${processId}`,
    {
      headers: {
        'Authorization': `Bearer ${token}`,
        'Content-Type': 'application/json'
      }
    }
  );
  
  if (response.ok) {
    const result = await response.json();
    setProcessReport(result.data);
  }
};
```

## 🎨 أمثلة على التصميم

### بطاقة عملية
```tsx
<button className="bg-white rounded-lg shadow-sm p-6 hover:shadow-md">
  <div className="flex items-center space-x-4">
    <div className="w-12 h-12 bg-blue-500 rounded-lg">
      <span className="text-white font-bold">ع</span>
    </div>
    <div>
      <h3 className="font-bold text-gray-900">عملية جديدة</h3>
      <p className="text-sm text-gray-500">وصف العملية</p>
    </div>
  </div>
</button>
```

### بطاقة إحصائية
```tsx
<div className="bg-white rounded-lg shadow-sm p-6">
  <div className="flex items-center justify-between">
    <div>
      <p className="text-sm font-medium text-gray-600">إجمالي التذاكر</p>
      <p className="text-3xl font-bold text-gray-900">150</p>
    </div>
    <div className="p-3 bg-blue-100 rounded-lg">
      <BarChart3 className="w-6 h-6 text-blue-600" />
    </div>
  </div>
</div>
```

## 🚀 الميزات المستقبلية

### تبويبة المستخدمين
- تقرير شامل لكل مستخدم
- استدعاء `GET /api/reports/user/{user_id}`
- إحصائيات الأداء الفردي
- التذاكر المسندة والمكتملة

### تبويبة قيد التطوير
- تقارير مخصصة
- تصدير التقارير (PDF, Excel)
- رسوم بيانية تفاعلية
- مقارنات بين الفترات الزمنية

## 📊 البيانات المعروضة

### الإحصائيات الأساسية
| المؤشر | الوصف | اللون |
|--------|-------|-------|
| إجمالي التذاكر | عدد جميع التذاكر | أزرق |
| مكتملة | التذاكر المكتملة + معدل الإنجاز | أخضر |
| متأخرة | التذاكر المتأخرة + النسبة | أحمر |
| متوسط الإنجاز | متوسط الوقت بالساعات | بنفسجي |

### توزيع المراحل
- **ديناميكي**: يعرض جميع المراحل الموجودة في العملية
- **مرئي**: شريط تقدم لكل مرحلة
- **مفصل**: العدد والنسبة المئوية

### توزيع الأولوية
- عاجل جداً (Urgent)
- عاجل (High)
- متوسط (Medium)
- منخفض (Low)

## 🔧 التخصيص

### تغيير الألوان
```tsx
const getPriorityColor = (priority: string) => {
  switch (priority.toLowerCase()) {
    case 'urgent': return 'bg-red-500';
    case 'high': return 'bg-orange-500';
    case 'medium': return 'bg-yellow-500';
    case 'low': return 'bg-green-500';
    default: return 'bg-gray-500';
  }
};
```

### إضافة فلاتر زمنية
```tsx
// يمكن إضافة فلاتر للفترة الزمنية
const [dateRange, setDateRange] = useState({
  from: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000),
  to: new Date()
});

// ثم تمريرها كـ query parameters
const url = `http://localhost:3000/api/reports/process/${processId}?date_from=${dateRange.from}&date_to=${dateRange.to}`;
```

## 🎯 نصائح للمدير

### كيفية قراءة التقارير

1. **الإحصائيات الأساسية**
   - انظر إلى معدل الإنجاز (Completion Rate)
   - إذا كان أقل من 50%، قد تحتاج إلى تحسين العملية

2. **توزيع المراحل**
   - إذا كانت معظم التذاكر في مرحلة واحدة، قد يكون هناك اختناق (Bottleneck)
   - التوزيع المتوازن يدل على سير عمل صحي

3. **التذاكر المتأخرة**
   - نسبة عالية من التذاكر المتأخرة تدل على مشاكل في التخطيط
   - راجع أسباب التأخير وحدد الحلول

4. **أفضل الموظفين**
   - استخدم هذه المعلومات للتقدير والمكافآت
   - تعلم من أساليب عمل الموظفين المتميزين

## ✅ الحالة

**تم التطبيق بنجاح** - النظام جاهز للاستخدام! 🎉

### ما تم إنجازه
- ✅ ثلاث تبويبات منظمة
- ✅ جلب العمليات من API
- ✅ عرض قائمة العمليات
- ✅ جلب تقرير العملية التفصيلي
- ✅ عرض الإحصائيات الأساسية
- ✅ توزيع المراحل والأولويات
- ✅ أفضل الموظفين أداءً
- ✅ تصميم احترافي وجميل
- ✅ تجربة مستخدم سلسة

### قيد التطوير
- ⏳ تبويبة المستخدمين
- ⏳ فلاتر زمنية متقدمة
- ⏳ تصدير التقارير
- ⏳ رسوم بيانية تفاعلية
