# 📦 دليل التثبيت - نظام Pipefy

## 🚀 التثبيت السريع

### المتطلبات الأساسية:
- Node.js (v14 أو أحدث)
- PostgreSQL (v12 أو أحدث)
- npm أو yarn

---

## 📋 خطوات التثبيت

### 1️⃣ تثبيت الحزم

```bash
npm install
```

---

### 2️⃣ إعداد متغيرات البيئة

```bash
cp .env.example .env
```

حدّث ملف `.env` بإعدادات قاعدة البيانات الخاصة بك:

```env
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=pipefy-main
DB_USERNAME=postgres
DB_PASSWORD=your_password

JWT_SECRET=your_secret_key
PORT=3003
```

---

### 3️⃣ إنشاء قاعدة البيانات

```bash
npm run create-db
```

---

### 4️⃣ تشغيل الـ Migrations

```bash
npm run migrate
```

---

### 5️⃣ إنشاء مستخدم Super Admin ⭐

```bash
node scripts/create-admin.js
```

**هذا الأمر سيقوم بـ:**
- ✅ إنشاء مستخدم `admin@pipefy.com`
- ✅ منحه **جميع الصلاحيات** تلقائياً (34+ صلاحية)
- ✅ تفعيل الحساب فوراً
- ✅ جعله جاهز للاستخدام

**معلومات الدخول الافتراضية:**
```
📧 البريد: admin@pipefy.com
🔑 كلمة المرور: admin123
```

---

## 🎯 التشغيل

### وضع التطوير:
```bash
npm run dev
```

### وضع الإنتاج:
```bash
npm start
```

---

## 🧪 الاختبار

### 1. التحقق من اتصال قاعدة البيانات:
```bash
curl http://localhost:3003/test-db
```

### 2. الوصول إلى API:
```bash
curl http://localhost:3003/api
```

### 3. توثيق Swagger:
```
http://localhost:3003/api-docs
```

### 4. تسجيل الدخول:
```bash
curl -X 'POST' \
  'http://localhost:3003/api/auth/login' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "email": "admin@pipefy.com",
  "password": "admin123"
}'
```

**النتيجة المتوقعة:**
```json
{
  "success": true,
  "data": {
    "user": {
      "id": "...",
      "name": "مدير النظام الرئيسي",
      "email": "admin@pipefy.com",
      "role": {
        "name": "Admin"
      },
      "permissions": [
        // جميع الصلاحيات (34+ صلاحية)
      ]
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

---

## 🔐 الصلاحيات المُمنوحة للـ Super Admin

المستخدم الذي يتم إنشاؤه سيحصل على **جميع** الصلاحيات:

### إدارة المستخدمين:
- ✅ عرض، إنشاء، تعديل، حذف المستخدمين
- ✅ إدارة حسابات المستخدمين

### إدارة الأدوار والصلاحيات:
- ✅ عرض وإدارة الأدوار
- ✅ إدارة الصلاحيات

### إدارة العمليات:
- ✅ قراءة، إنشاء، تحديث، حذف العمليات
- ✅ إدارة العمليات والمراحل

### إدارة التذاكر:
- ✅ عرض جميع التذاكر
- ✅ إنشاء، تعديل، حذف التذاكر
- ✅ إدارة كاملة للتذاكر

### صلاحيات أخرى:
- ✅ عرض التقارير والإحصائيات
- ✅ إدارة التكاملات الخارجية
- ✅ تعديل إعدادات النظام

**المجموع:** 34+ صلاحية تغطي جميع جوانب النظام

---

## 📊 ملخص الأوامر

```bash
# التثبيت الكامل
npm install
cp .env.example .env
# حدّث .env
npm run create-db
npm run migrate
node scripts/create-admin.js

# التشغيل
npm run dev

# الاختبار
curl http://localhost:3003/test-db
curl http://localhost:3003/api-docs
```

---

## ⚠️ ملاحظات مهمة

### 1. تغيير كلمة المرور:
**مهم جداً:** غيّر كلمة المرور الافتراضية بعد أول تسجيل دخول!

### 2. البيئة الإنتاجية:
- لا تستخدم كلمة المرور الافتراضية `admin123`
- غيّر `JWT_SECRET` إلى قيمة عشوائية قوية
- فعّل HTTPS
- استخدم متغيرات بيئة آمنة

### 3. إعادة إنشاء المستخدم:
إذا احتجت إعادة إنشاء المستخدم:
```bash
node scripts/create-admin.js
```
السكريبت سيحذف المستخدم القديم ويُنشئ واحد جديد تلقائياً.

---

## 🐛 استكشاف الأخطاء

### خطأ: "Admin role not found"
```bash
# تأكد من تشغيل migrations
npm run migrate
```

### خطأ: "Connection refused"
```bash
# تأكد من تشغيل PostgreSQL
# تحقق من إعدادات .env
```

### خطأ: "Database does not exist"
```bash
# أنشئ قاعدة البيانات
npm run create-db
```

---

## 📚 الموارد

- **Swagger UI:** `http://localhost:3003/api-docs`
- **Test DB:** `http://localhost:3003/test-db`
- **API Base:** `http://localhost:3003/api`

---

## ✅ قائمة التحقق

- [ ] تثبيت Node.js و PostgreSQL
- [ ] تشغيل `npm install`
- [ ] إنشاء وتحديث ملف `.env`
- [ ] تشغيل `npm run create-db`
- [ ] تشغيل `npm run migrate`
- [ ] تشغيل `node scripts/create-admin.js`
- [ ] تشغيل السيرفر `npm run dev`
- [ ] اختبار تسجيل الدخول
- [ ] تغيير كلمة المرور الافتراضية

---

## 🎉 النتيجة

بعد إكمال جميع الخطوات:
- ✅ النظام يعمل على `http://localhost:3003`
- ✅ مستخدم Super Admin جاهز
- ✅ جميع الصلاحيات مُمنوحة (34+ صلاحية)
- ✅ يمكنك إدارة كل شيء في النظام

**النظام جاهز للعمل!** 🚀
