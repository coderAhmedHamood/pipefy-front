# 🚀 Quick Start - إنشاء والتحقق من مستخدم Admin

## الخطوة 1: إنشاء المستخدم والعملية

```bash
cd api
node scripts/create-admin.js
```

**ماذا يفعل:**
- ✅ ينشئ مستخدم admin (admin@pipefy.com / admin123)
- ✅ ينشئ عملية رئيسية
- ✅ **يربط المستخدم بالعملية عبر UserProcess Model الرسمي**
- ✅ يعطي جميع الصلاحيات

---

## الخطوة 2: التحقق من الربط

```bash
node scripts/verify-admin-process-link.js
```

**ماذا يفعل:**
- ✅ يسجل دخول المستخدم
- ✅ يجلب العمليات المرتبطة به
- ✅ يتحقق من جدول user_processes
- ✅ يعرض تقرير كامل

---

## النتيجة المتوقعة ✅

```
✅ المستخدم: مدير النظام العام
✅ البريد: admin@pipefy.com
✅ كلمة المرور: admin123
✅ العملية: العملية الرئيسية
✅ الدور: admin
✅ الحالة: نشط
```

---

## اختبار سريع عبر API

### 1. تسجيل الدخول

```bash
curl -X POST http://localhost:3004/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@pipefy.com","password":"admin123"}'
```

**احفظ الـ token من الاستجابة!**

### 2. جلب العمليات

```bash
curl -X GET "http://localhost:3004/api/users/{USER_ID}/processes" \
  -H "Authorization: Bearer {TOKEN}"
```

**يجب أن تظهر "العملية الرئيسية" في الاستجابة!**

---

## إذا واجهت مشاكل

### المستخدم لا يرى العمليات؟

```bash
# أعد تشغيل السكربت
node scripts/create-admin.js
```

### تريد التأكد من الربط؟

```bash
# شغل سكربت التحقق
node scripts/verify-admin-process-link.js
```

---

## API Endpoints المهمة

| Endpoint | الوصف |
|----------|-------|
| `POST /api/auth/login` | تسجيل الدخول |
| `GET /api/users/{id}/processes` | جلب عمليات المستخدم |
| `GET /api/user-processes?user_id={id}` | جلب الروابط |
| `POST /api/user-processes` | إنشاء ربط جديد |

---

## معلومات تسجيل الدخول

```
📧 البريد: admin@pipefy.com
🔑 كلمة المرور: admin123
```

---

## للمزيد

اقرأ الملفات التالية:
- `USER_PROCESS_LINK_VERIFICATION.md` - توثيق شامل
- `api/USER_PROCESS_SYSTEM_IMPLEMENTATION.md` - نظام user_processes
- `api/USER_PROCESS_ENDPOINTS_GUIDE.md` - دليل API endpoints

