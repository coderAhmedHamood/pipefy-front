# تحديث عرض صافي الأداء (Net Performance Hours)

## التحديثات المطبقة ✅

### 1. **عرض ديناميكي من قاعدة البيانات**
- الكود يجلب البيانات بشكل ديناميكي من API: `GET /api/reports/process/{process_id}`
- القيمة `net_performance_hours` تأتي من قاعدة البيانات مباشرة
- يتم تحديث البيانات عند تغيير الفترة الزمنية

### 2. **تحويل الساعات إلى أيام وساعات**
```typescript
const hours = parseFloat(processReport.performance_metrics.net_performance_hours);
const absHours = Math.abs(hours);
const days = Math.floor(absHours / 24);
const remainingHours = Math.round(absHours % 24);
```

**أمثلة:**
- `-356.29` ساعة → **-14 يوم و 20 ساعة**
- `+48.5` ساعة → **+2 يوم و 1 ساعة**
- `+15.3` ساعة → **+15.3 ساعة** (أقل من 24 ساعة)

### 3. **نظام التلوين الذكي**

#### أ) خلفية البطاقة:
- **موجب (متقدم)**: `bg-gradient-to-br from-green-50 to-green-100`
- **سالب (متأخر)**: `bg-gradient-to-br from-red-50 to-red-100`
- **صفر (حسب الجدول)**: `bg-gradient-to-br from-gray-50 to-gray-100`

#### ب) لون النص:
- **موجب**: `text-green-700`
- **سالب**: `text-red-700`
- **صفر**: `text-gray-700`

#### ج) أيقونة الأداء:
- **موجب**: خلفية خضراء `bg-green-200` + أيقونة خضراء
- **سالب**: خلفية حمراء `bg-red-200` + أيقونة حمراء
- **صفر**: خلفية رمادية `bg-gray-200` + أيقونة رمادية

### 4. **رسائل توضيحية**
- **✅ متقدم عن الجدول** - للقيم الموجبة
- **⚠️ متأخر عن الجدول** - للقيم السالبة
- **⏱️ حسب الجدول** - للقيمة صفر

## مثال على البيانات

### البيانات من API:
```json
{
  "performance_metrics": {
    "net_performance_hours": "-356.29"
  }
}
```

### العرض في الواجهة:
```
┌─────────────────────────────────────┐
│ صافي الأداء                         │
│                                     │
│ -14 يوم و 20 ساعة                  │ (أحمر غامق)
│ ⚠️ متأخر عن الجدول                 │
│                                     │
│                        [📊]         │ (أيقونة حمراء)
└─────────────────────────────────────┘
(خلفية حمراء فاتحة)
```

## الملفات المتأثرة

### `src/components/reports/ReportsManager.tsx`
- **السطر 601-604**: حساب الأيام والساعات
- **السطر 610-618**: عرض القيمة بالتنسيق المناسب
- **السطر 585-591**: تلوين الخلفية
- **السطر 611-616**: تلوين النص
- **السطر 626-632**: تلوين الأيقونة

## كيفية الاستخدام

### 1. جلب التقرير:
```typescript
const fetchProcessReport = async (processId: string) => {
  const response = await fetch(
    `${API_BASE_URL}/api/reports/process/${processId}?date_from=${from}&date_to=${to}`
  );
  const result = await response.json();
  setProcessReport(result.data);
};
```

### 2. عرض البيانات:
- يتم عرض البيانات تلقائياً عند تحديد عملية
- يتم تحديث البيانات عند تغيير الفترة الزمنية
- التلوين والتنسيق يتم تلقائياً بناءً على القيمة

## الميزات الإضافية

### ✅ تحديث تلقائي
- البيانات تتحدث مع قاعدة البيانات مباشرة
- لا توجد قيم ثابتة أو مكتوبة يدوياً

### ✅ تنسيق ذكي
- عرض بالأيام والساعات إذا تجاوزت 24 ساعة
- عرض بالساعات فقط إذا كانت أقل من 24 ساعة

### ✅ تجربة مستخدم محسنة
- ألوان واضحة ومعبرة
- رسائل توضيحية
- تصميم جميل ومتناسق

## النتيجة النهائية

✅ **صافي الأداء يُقرأ من قاعدة البيانات بشكل ديناميكي**
✅ **تحويل تلقائي إلى أيام وساعات**
✅ **تلوين ذكي (أخضر للموجب، أحمر للسالب)**
✅ **تصميم جميل ومتجاوب**
✅ **رسائل توضيحية واضحة**

---

**تاريخ التحديث**: 2025-10-17  
**الحالة**: ✅ مكتمل وجاهز للاستخدام
