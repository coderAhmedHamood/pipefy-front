# تكامل إعدادات النظام العامة مع الواجهة

## نظرة عامة
تم إنشاء نظام شامل لجلب وعرض إعدادات النظام (اسم الشركة والشعار) من `GET /api/settings` وعرضها في جميع أنحاء التطبيق بشكل رسمي.

## المكونات الجديدة

### 1. SystemSettingsContext ✅
**الملف**: `src/contexts/SystemSettingsContext.tsx`

**الميزات**:
- جلب إعدادات النظام من `GET /api/settings` تلقائياً
- تحديث الإعدادات محلياً بدون استدعاء API
- Hook مبسط `useCompanyInfo` للحصول على اسم الشركة والشعار

**الاستخدام**:
```typescript
// للحصول على جميع الإعدادات
const { settings, loading, refreshSettings, updateSettings } = useSystemSettings();

// للحصول على اسم الشركة والشعار فقط
const { companyName, companyLogo, loading } = useCompanyInfo();
```

### 2. CompanyHeader Component ✅
**الملف**: `src/components/layout/CompanyHeader.tsx`

**الميزات**:
- عرض اسم الشركة والشعار من إعدادات النظام
- أحجام مختلفة: `small`, `medium`, `large`
- إمكانية إخفاء/إظهار الاسم أو الشعار
- حالة تحميل مع skeleton loading
- شعار افتراضي إذا لم يكن هناك شعار مخصص

**الاستخدام**:
```tsx
<CompanyHeader size="medium" showName={true} showLogo={true} />
<CompanyHeader size="small" showName={false} showLogo={true} />
```

## التحديثات المطبقة

### 1. App.tsx ✅
- إضافة `SystemSettingsProvider` إلى شجرة المكونات
- استبدال النص الثابت بـ `CompanyHeader`
- تنظيف الاستيرادات غير المستخدمة

```tsx
function App() {
  return (
    <Router>
      <AuthProvider>
        <SystemSettingsProvider>  {/* ✅ جديد */}
          <WorkflowProvider>
            <NotificationProvider>
              <ProtectedRoutes />
            </NotificationProvider>
          </WorkflowProvider>
        </SystemSettingsProvider>
      </AuthProvider>
    </Router>
  );
}
```

### 2. SettingsManager ✅
**التحديثات**:
- تحديث إعدادات النظام العامة عند حفظ الإعدادات
- تحديث الشعار في Header فور رفع شعار جديد
- تكامل مع `SystemSettingsContext`

```typescript
// عند حفظ الإعدادات
updateSystemSettings({
  company_name: response.data.company_name || '',
  company_logo: response.data.company_logo || ''
});

// عند رفع شعار جديد
updateSystemSettings({
  company_logo: logoUrl
});
```

## التدفق الكامل

### 1. عند تحميل التطبيق
```
App.tsx يحمل → 
  SystemSettingsProvider يستدعي GET /api/settings → 
    CompanyHeader يعرض اسم الشركة والشعار
```

### 2. عند تعديل الإعدادات
```
المستخدم يحفظ الإعدادات → 
  PUT /api/settings → 
    تحديث إعدادات النظام العامة → 
      Header يتحدث فوراً
```

### 3. عند رفع شعار جديد
```
المستخدم يرفع شعار → 
  POST /api/settings/logo → 
    تحديث شعار النظام → 
      Header يعرض الشعار الجديد فوراً
```

## الميزات الرئيسية

### 1. تحديث فوري ✅
- اسم الشركة والشعار يتحدثان فوراً عند الحفظ
- لا حاجة لإعادة تحميل الصفحة
- تحديث في جميع أجزاء التطبيق

### 2. حالات التحميل ✅
- Skeleton loading أثناء جلب الإعدادات
- مؤشرات تحميل واضحة
- معالجة حالات الخطأ

### 3. شعار افتراضي ✅
- إذا لم يكن هناك شعار مخصص، يظهر شعار افتراضي
- يستخدم أول حرف من اسم الشركة
- تصميم جميل مع gradient

### 4. أحجام متعددة ✅
- `small`: للـ sidebar المطوي
- `medium`: للـ header الرئيسي
- `large`: للصفحات الخاصة

## البيانات المُرجعة من GET /api/settings

```json
{
  "success": true,
  "data": {
    "company_name": "شركة المثال",
    "company_logo": "https://example.com/logo.png",
    "login_attempts_limit": 5,
    "lockout_duration_minutes": 30,
    "smtp_server": "smtp.gmail.com",
    "smtp_port": 587,
    "smtp_username": "user@example.com",
    "smtp_password": "password123"
  }
}
```

## الملفات الجديدة
- `src/contexts/SystemSettingsContext.tsx` - Context للإعدادات العامة
- `src/components/layout/CompanyHeader.tsx` - مكون عرض اسم الشركة والشعار

## الملفات المعدلة
- `src/App.tsx` - إضافة SystemSettingsProvider واستخدام CompanyHeader
- `src/components/settings/SettingsManagerUltraSimple.tsx` - تحديث الإعدادات العامة

## كيفية الاختبار

### 1. اختبار جلب الإعدادات:
1. افتح التطبيق
2. تحقق من ظهور اسم الشركة والشعار في Header
3. تحقق من Console للـ logs

### 2. اختبار التحديث الفوري:
1. اذهب لصفحة الإعدادات
2. غيّر اسم الشركة
3. احفظ الإعدادات
4. تحقق من تحديث Header فوراً

### 3. اختبار رفع الشعار:
1. ارفع شعار جديد
2. تحقق من ظهوره في Header فوراً
3. تحقق من المعاينة والتكبير

## Console Logs المتوقعة

```
🔄 جلب إعدادات النظام من GET /api/settings...
✅ تم جلب إعدادات النظام: {company_name: "شركة المثال", ...}
🌐 تحديث إعدادات النظام العامة في Header...
🌐 تحديث شعار النظام في Header...
```

## النتيجة النهائية
✅ اسم الشركة والشعار يظهران في جميع أنحاء التطبيق
✅ تحديث فوري عند تغيير الإعدادات
✅ جلب تلقائي من `GET /api/settings`
✅ تصميم جميل مع حالات التحميل
✅ نظام مرن وقابل للتوسع
